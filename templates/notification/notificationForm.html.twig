{{ form_start(form, {'attr': {'class': 'notificationForm'}}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.day) }}
    {{ form_row(form.description) }}
<button type="submit" class="btn btn-primary mt-3 notificationCreateSubmitButton">Dodaj</button>
{{ form_end(form) }}